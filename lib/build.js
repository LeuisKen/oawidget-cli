/**
 * @file build, build the src files to dist by webpack
 * @author X-Jray(z.xuanjian@gmail.com)
*/

function main(cmptName) {
    console.log('I\'m build component ' + cmptName);
}

module.exports = main;
