/**
 * @file publish, publish the dist to server
 * @author X-Jray(z.xuanjian@gmail.com)
*/

function main(cmptName) {
    console.log('I\'m publish component ' + cmptName);
}

module.exports = main;
