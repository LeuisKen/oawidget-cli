/**
 * @file `${cmptName}` entry
 * @author YourName(YourName@baidu.com)
 */

import ${cmptName} from './${cmptName}';
import schema from './${cmptName}.editor';

export default {
    widget: ${cmptName},
    schema
};
